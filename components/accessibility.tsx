"use client"; import { useEffect,useState } from "react";
export function AccessibilityControls(){const [size,setSize]=useState(100);useEffect(()=>{const saved=localStorage.getItem('aacans-font');if(saved)setSize(Number(saved))},[]);useEffect(()=>{document.documentElement.style.fontSize=`${size}%`;localStorage.setItem('aacans-font',String(size))},[size]);return <div className="access" aria-label="Controles de acessibilidade"><button onClick={()=>setSize(Math.min(115,size+5))} aria-label="Aumentar fonte">A+</button><button onClick={()=>setSize(100)} aria-label="Restaurar fonte">A</button><button onClick={()=>setSize(Math.max(90,size-5))} aria-label="Diminuir fonte">A−</button></div>}
