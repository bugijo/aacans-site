import Link from "next/link"; import { ArrowRight, FileText } from "lucide-react"; import { CTA } from "@/components/site";
export function PageHero({eyebrow,title,children}:{eyebrow:string;title:string;children:string}){return <section className="page-hero"><div className="container"><div className="eyebrow">{eyebrow}</div><h1 style={{fontSize:'clamp(2.7rem,6vw,4.7rem)',marginTop:14}}>{title}</h1><p>{children}</p></div></section>}
export function InfoCard({icon:Icon,title,text,href}:{icon:typeof FileText;title:string;text:string;href?:string}){return <article className="card"><div className="icon"><Icon size={20}/></div><h3>{title}</h3><p>{text}</p>{href&&<Link className="btn btn-secondary" style={{marginTop:18}} href={href}>Saiba mais <ArrowRight size={16}/></Link>}</article>}
export function Empty({title='Conteúdo em preparação.',text='Novidades serão publicadas aqui em breve.'}){return <div className="empty"><h3>{title}</h3><p>{text}</p></div>}
export function Inner({eyebrow,title,description,children,cta=true}:{eyebrow:string;title:string;description:string;children:React.ReactNode;cta?:boolean}){return <><PageHero eyebrow={eyebrow} title={title}>{description}</PageHero><section className="section"><div className="container">{children}</div></section>{cta&&<CTA/>}</>}
