import type { MetadataRoute } from 'next'; export const dynamic='force-static'; const routes=['','quem-somos','como-ajudamos','pacientes-e-familiares','quero-ajudar','voluntariado','parceiros','projetos','noticias','eventos','transparencia','contato','privacidade','termos','doar']; export default function sitemap():MetadataRoute.Sitemap{return routes.map(route=>({url:`https://bugijo.github.io/aacans-site/${route}`,lastModified:new Date()}))}
